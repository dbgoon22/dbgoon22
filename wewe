--// dbgoon22 CLIENT - 333 Mejorado + Speed //--
-- ESP Negro + Speed + Hitbox Moderada

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local AimAssistEnabled = false
local ESPEnabled = false
local TriggerBotEnabled = false
local HitboxEnabled = false
local RainbowHitbox = false
local SpeedEnabled = false

local AimPower = 96
local lastShot = 0
local SHOT_COOLDOWN = 0.075
local WalkSpeed = 50  -- Velocidad normal alta

local originalSizes = {}
local originalWalkSpeed = 16

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 380, 0, 600)
MainFrame.Position = UDim2.new(0.08, 0, 0.05, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 65)
Title.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
Title.Text = "dbgoon22 333 - Hitbox + Speed"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 24
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 14)

local function CreateButton(text, yPos, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.9, 0, 0, 52)
    Button.Position = UDim2.new(0.05, 0, 0, yPos)
    Button.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 17
    Button.Font = Enum.Font.GothamSemibold
    Button.Parent = MainFrame
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 10)
    
    Button.MouseButton1Click:Connect(function()
        callback(Button)
    end)
    return Button
end

local function IsVisible(target)
    if not target or not target.Parent then return false end
    local character = target.Parent
    local root = character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("Head")
    if not root then return false end
    
    local ray = Ray.new(Camera.CFrame.Position, (root.Position - Camera.CFrame.Position).Unit * 4500)
    local ignoreList = {LocalPlayer.Character, Workspace.CurrentCamera, Workspace.Terrain}
    local hit = Workspace:FindPartOnRayWithIgnoreList(ray, ignoreList)
    return hit == nil or hit:IsDescendantOf(character)
end

local function GetClosestTarget()
    local closest, dist = nil, math.huge
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local root = plr.Character:FindFirstChild("HumanoidRootPart") or plr.Character:FindFirstChild("Head")
            if root and IsVisible(root) then
                local pos, onScreen = Camera:WorldToViewportPoint(root.Position)
                if onScreen then
                    local d = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                    if d < dist then
                        dist = d
                        closest = root
                    end
                end
            end
        end
    end
    return closest
end

-- AimAssist (solo con Hitbox)
RunService.RenderStepped:Connect(function()
    if not AimAssistEnabled or not HitboxEnabled then return end
    local target = GetClosestTarget()
    if target then
        local cf = Camera.CFrame
        Camera.CFrame = cf:Lerp(CFrame.lookAt(cf.Position, target.Position), AimPower/106)
    end
end)

-- ESP Negro
RunService.RenderStepped:Connect(function()
    if not ESPEnabled then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character and not plr.Character:FindFirstChild("HighlightESP") then
            local hl = Instance.new("Highlight")
            hl.Name = "HighlightESP"
            hl.FillTransparency = 0.7
            hl.OutlineColor = Color3.fromRGB(255, 0, 0)     -- Outline rojo
            hl.OutlineTransparency = 0.1
            hl.FillColor = Color3.fromRGB(0, 0, 0)         -- Negro
            hl.Parent = plr.Character
        end
    end
end)

-- Hitbox Moderada
local rainbowHue = 0
RunService.RenderStepped:Connect(function()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
            local head = plr.Character:FindFirstChild("Head")
            
            if hrp and not originalSizes[hrp] then originalSizes[hrp] = hrp.Size end
            if head and not originalSizes[head] then originalSizes[head] = head.Size end

            if HitboxEnabled then
                rainbowHue = (rainbowHue + 0.028) % 1
                local color = RainbowHitbox and Color3.fromHSV(rainbowHue, 1, 1) or Color3.fromRGB(255, 60, 60)
                
                if hrp then
                    hrp.Size = Vector3.new(11, 11, 11)
                    hrp.Transparency = 0.25
                    hrp.Color = color
                    hrp.CanCollide = false
                    hrp.Material = Enum.Material.ForceField
                end
                if head then
                    head.Size = Vector3.new(6.5, 6.5, 6.5)
                    head.Transparency = 0.25
                    head.Color = color
                    head.Material = Enum.Material.ForceField
                end
            else
                if hrp then
                    hrp.Size = originalSizes[hrp] or Vector3.new(2, 2, 1)
                    hrp.Transparency = 0
                    hrp.Color = Color3.fromRGB(163, 162, 165)
                    hrp.Material = Enum.Material.Plastic
                end
                if head then
                    head.Size = originalSizes[head] or Vector3.new(1, 1, 1)
                    head.Transparency = 0
                    head.Color = Color3.fromRGB(163, 162, 165)
                    head.Material = Enum.Material.Plastic
                end
            end
        end
    end
end)

-- TriggerBot (solo con Hitbox)
RunService.RenderStepped:Connect(function()
    if not TriggerBotEnabled or not HitboxEnabled then return end
    if tick() - lastShot < SHOT_COOLDOWN then return end
    
    local target = GetClosestTarget()
    if target then
        local pos, onScreen = Camera:WorldToViewportPoint(target.Position)
        if onScreen then
            local distance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
            if distance < 110 then
                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
                task.wait(0.00001)
                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
                lastShot = tick()
            end
        end
    end
end)

-- Speed
RunService.RenderStepped:Connect(function()
    if not SpeedEnabled then return end
    local character = LocalPlayer.Character
    if character then
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = WalkSpeed
        end
    end
end)

-- Botones
CreateButton("Aim Assist: OFF", 80, function(self)
    AimAssistEnabled = not AimAssistEnabled
    self.Text = "Aim Assist: " .. (AimAssistEnabled and "ON" or "OFF")
    self.BackgroundColor3 = AimAssistEnabled and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 50)
end)

CreateButton("ESP Negro: OFF", 145, function(self)
    ESPEnabled = not ESPEnabled
    self.Text = "ESP Negro: " .. (ESPEnabled and "ON" or "OFF")
    self.BackgroundColor3 = ESPEnabled and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 50)
end)

CreateButton("TriggerBot: OFF", 210, function(self)
    TriggerBotEnabled = not TriggerBotEnabled
    self.Text = "TriggerBot: " .. (TriggerBotEnabled and "ON" or "OFF")
    self.BackgroundColor3 = TriggerBotEnabled and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 50)
end)

CreateButton("Hitbox Moderada: OFF", 275, function(self)
    HitboxEnabled = not HitboxEnabled
    self.Text = "Hitbox Moderada: " .. (HitboxEnabled and "ON" or "OFF")
    self.BackgroundColor3 = HitboxEnabled and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 50)
end)

CreateButton("Rainbow Hitbox: OFF", 340, function(self)
    RainbowHitbox = not RainbowHitbox
    self.Text = "Rainbow Hitbox: " .. (RainbowHitbox and "ON" or "OFF")
    self.BackgroundColor3 = RainbowHitbox and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 50)
end)

CreateButton("Speed: OFF", 405, function(self)
    SpeedEnabled = not SpeedEnabled
    self.Text = "Speed: " .. (SpeedEnabled and "ON" or "OFF")
    self.BackgroundColor3 = SpeedEnabled and Color3.fromRGB(0, 200, 0) or Color3.fromRGB(40, 40, 50)
end)

UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.P then
        MainFrame.Visible = not MainFrame.Visible
    end
    if input.KeyCode == Enum.KeyCode.V then
        SpeedEnabled = not SpeedEnabled
        print("Speed: " .. (SpeedEnabled and "ON" or "OFF"))
    end
end)

print("✅ dbgoon22 333 Mejorado | ESP Negro + Speed (V) | Presiona P para menú")
