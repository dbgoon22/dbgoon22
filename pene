--// dbgoon22 CLIENT - PERFECT BYPASS //--

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local AimAssistEnabled = false
local ESPEnabled = false
local TriggerBotEnabled = false
local HitboxEnabled = false

local AimPower = 94
local HitboxSize = 12.5   -- Mejor tamaño

local originalSizes = {}
local lastShot = 0

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 370, 0, 480)
MainFrame.Position = UDim2.new(0.08, 0, 0.05, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 65)
Title.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
Title.Text = "dbgoon22 Perfect Bypass"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 24
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 14)

local function CreateButton(text, yPos, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.9, 0, 0, 52)
    Button.Position = UDim2.new(0.05, 0, 0, yPos)
    Button.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 17
    Button.Font = Enum.Font.GothamSemibold
    Button.Parent = MainFrame
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 10)
    
    Button.MouseButton1Click:Connect(function()
        callback(Button)
    end)
    return Button
end

local function UpdateButton(button, enabled)
    if enabled then
        button.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
        button.Text = button.Text:gsub(": OFF", ": ON")
    else
        button.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
        button.Text = button.Text:gsub(": ON", ": OFF")
    end
end

local function IsVisible(target)
    if not target or not target.Parent then return false end
    local character = target.Parent
    local root = character:FindFirstChild("HumanoidRootPart")
    if not root then return false end
    
    local ray = Ray.new(Camera.CFrame.Position, (root.Position - Camera.CFrame.Position).Unit * 6000)
    local ignoreList = {LocalPlayer.Character, Workspace.CurrentCamera, Workspace.Terrain}
    local hit = Workspace:FindPartOnRayWithIgnoreList(ray, ignoreList)
    return hit == nil or hit:IsDescendantOf(character)
end

local function GetClosestTarget()
    local closest, dist = nil, math.huge
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local root = plr.Character:FindFirstChild("HumanoidRootPart")
            if root and IsVisible(root) then
                local pos, onScreen = Camera:WorldToViewportPoint(root.Position)
                if onScreen then
                    local d = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                    if d < dist then
                        dist = d
                        closest = root
                    end
                end
            end
        end
    end
    return closest
end

-- Aim Assist Mejorado
RunService.RenderStepped:Connect(function()
    if not AimAssistEnabled then return end
    local target = GetClosestTarget()
    if target then
        local cf = Camera.CFrame
        local targetPos = target.Position + Vector3.new(0, 0.4, 0)
        Camera.CFrame = cf:Lerp(CFrame.lookAt(cf.Position, targetPos), AimPower / 107)
    end
end)

-- ESP Negro Puro
RunService.RenderStepped:Connect(function()
    if not ESPEnabled then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local root = plr.Character:FindFirstChild("HumanoidRootPart")
            if root and not root:FindFirstChild("BlackESP") then
                local hl = Instance.new("Highlight")
                hl.Name = "BlackESP"
                hl.FillTransparency = 0.85
                hl.OutlineColor = Color3.fromRGB(255, 30, 30)
                hl.OutlineTransparency = 0.05
                hl.FillColor = Color3.fromRGB(0, 0, 0)
                hl.Parent = root
            end
        end
    end
end)

-- Hitbox Mejorada (Solo Cuerpo)
RunService.RenderStepped:Connect(function()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
            if hrp and not originalSizes[hrp] then originalSizes[hrp] = hrp.Size end

            if HitboxEnabled then
                if hrp then
                    hrp.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
                    hrp.Transparency = 0.22
                    hrp.Color = Color3.fromRGB(255, 50, 50)
                    hrp.CanCollide = false
                    hrp.Material = Enum.Material.ForceField
                end
            else
                if hrp then
                    hrp.Size = originalSizes[hrp] or Vector3.new(2, 2, 1)
                    hrp.Transparency = 0
                    hrp.Color = Color3.fromRGB(163, 162, 165)
                    hrp.Material = Enum.Material.Plastic
                end
            end
        end
    end
end)

-- TriggerBot a 0.01ms
RunService.RenderStepped:Connect(function()
    if not TriggerBotEnabled then return end
    if tick() - lastShot < 0.00001 then return end   -- 0.01ms
    
    local target = GetClosestTarget()
    if target then
        local pos, onScreen = Camera:WorldToViewportPoint(target.Position)
        if onScreen then
            local distance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
            if distance < HitboxSize * 10.5 then
                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
                task.wait(0.000008)
                VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
                lastShot = tick()
            end
        end
    end
end)

-- Botones
local aimBtn = CreateButton("Aim Assist: OFF", 80, function(self)
    AimAssistEnabled = not AimAssistEnabled
    UpdateButton(self, AimAssistEnabled)
end)

local espBtn = CreateButton("ESP Black: OFF", 145, function(self)
    ESPEnabled = not ESPEnabled
    UpdateButton(self, ESPEnabled)
end)

local triggerBtn = CreateButton("TriggerBot 0.01ms: OFF", 210, function(self)
    TriggerBotEnabled = not TriggerBotEnabled
    UpdateButton(self, TriggerBotEnabled)
end)

local hitboxBtn = CreateButton("Hitbox Better: OFF", 275, function(self)
    HitboxEnabled = not Hit
