--// dbgoon22 CLIENT - POTASSIUM FINAL + MEJORAS //--

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local AimAssistEnabled = false
local ESPEnabled = false
local TriggerBotEnabled = false
local HitboxEnabled = false
local RainbowHitbox = false
local AimPower = 100

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 340, 0, 520)
MainFrame.Position = UDim2.new(0.08, 0, 0.08, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 60)
Title.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
Title.Text = "dbgoon22 Client"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 26
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 12)

local function CreateButton(text, yPos, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.9, 0, 0, 50)
    Button.Position = UDim2.new(0.05, 0, 0, yPos)
    Button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 16
    Button.Font = Enum.Font.GothamSemibold
    Button.Parent = MainFrame
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 10)
    Button.MouseButton1Click:Connect(callback)
    return Button
end

CreateButton("Aim Assist Head: ON",100 , function(self)
    AimAssistEnabled = not AimAssistEnabled
    self.Text = "Aim Assist Head: " .. (AimAssistEnabled and "ON" or "OFF")
end)

CreateButton("ESP + Wallhack: ON", 140, function(self)
    ESPEnabled = not ESPEnabled
    self.Text = "ESP + Wallhack: " .. (ESPEnabled and "ON" or "OFF")
end)

CreateButton("TriggerBot: OFF", 200, function(self)
    TriggerBotEnabled = not TriggerBotEnabled
    self.Text = "TriggerBot: " .. (TriggerBotEnabled and "ON" or "OFF")
end)

CreateButton("Hitbox Expand: ON", 450, function(self)
    HitboxEnabled = not HitboxEnabled
    self.Text = "Hitbox Expand: " .. (HitboxEnabled and "ON" or "OFF")
end)

CreateButton("Rainbow Hitbox: OFF", 320, function(self)
    RainbowHitbox = not RainbowHitbox
    self.Text = "Rainbow Hitbox: " .. (RainbowHitbox and "ON" or "OFF")
end)

-- AIM ASSIST MEJORADO
RunService.RenderStepped:Connect(function()
    if not AimAssistEnabled then return end
    local closest, dist = nil, math.huge
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("Head") then
            local head = plr.Character.Head
            local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
            if onScreen then
                local d = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if d < dist then 
                    dist = d 
                    closest = head.Position 
                end
            end
        end
    end
    if closest then
        local cf = Camera.CFrame
        Camera.CFrame = cf:Lerp(CFrame.lookAt(cf.Position, closest), AimPower/140)
    end
end)

-- ESP + WALLHACK
RunService.RenderStepped:Connect(function()
    if not ESPEnabled then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            if not plr.Character:FindFirstChild("HighlightESP") then
                local hl = Instance.new("Highlight")
                hl.Name = "HighlightESP"
                hl.FillTransparency = 0.7
                hl.OutlineColor = Color3.fromRGB(255, 50, 50)
                hl.FillColor = Color3.fromRGB(0, 255, 255)
                hl.Parent = plr.Character
            end
        end
    end
end)

-- HITBOX EXPAND (INSANE)
local rainbowHue = 0
RunService.RenderStepped:Connect(function()
    if not HitboxEnabled then return end
    rainbowHue = (rainbowHue + 0.01) % 1
    local color = RainbowHitbox and Color3.fromHSV(rainbowHue, 1, 1) or Color3.fromRGB(255, 0, 0)
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
            local head = plr.Character:FindFirstChild("Head")
            if hrp then
                hrp.Size = Vector3.new(10, 10, 10)
                hrp.Transparency = 0.6
                hrp.Color = color
                hrp.CanCollide = false
            end
            if head then
                head.Size = Vector3.new(6, 6, 6)
                head.Transparency = 0.6
                head.Color = color
            end
        end
    end
end)

-- TRIGGERBOT 0.1ms
RunService.RenderStepped:Connect(function()
    if not TriggerBotEnabled then return end
    for _, plr in ipairs(Players:GetPlayers())**✅ Actualizado con todo lo que pediste para `ritzy`**

Reemplaza **todo** el contenido de `ritzy` con este código mejorado:

```lua
--// dbgoon22 CLIENT - POTASSIUM FINAL + MEJORAS //--

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local VirtualInputManager = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

local AimAssistEnabled = false
local ESPEnabled = false
local TriggerBotEnabled = false
local HitboxEnabled = false
local RainbowHitbox = false
local AimPower = 75

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 340, 0, 520)
MainFrame.Position = UDim2.new(0.08, 0, 0.08, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 60)
Title.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
Title.Text = "dbgoon22 Client"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 26
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 12)

local function CreateButton(text, yPos, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.9, 0, 0, 50)
    Button.Position = UDim2.new(0.05, 0, 0, yPos)
    Button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 16
    Button.Font = Enum.Font.GothamSemibold
    Button.Parent = MainFrame
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 10)
    Button.MouseButton1Click:Connect(callback)
    return Button
end

CreateButton("Aim Assist Head: OFF", 80, function(self)
    AimAssistEnabled = not AimAssistEnabled
    self.Text = "Aim Assist Head: " .. (AimAssistEnabled and "ON" or "OFF")
end)

CreateButton("ESP + Wallhack: OFF", 140, function(self)
    ESPEnabled = not ESPEnabled
    self.Text = "ESP + Wallhack: " .. (ESPEnabled and "ON" or "OFF")
end)

CreateButton("TriggerBot: OFF", 200, function(self)
    TriggerBotEnabled = not TriggerBotEnabled
    self.Text = "TriggerBot: " .. (TriggerBotEnabled and "ON" or "OFF")
end)

CreateButton("Hitbox Expand: OFF", 260, function(self)
    HitboxEnabled = not HitboxEnabled
    self.Text = "Hitbox Expand: " .. (HitboxEnabled and "ON" or "OFF")
end)

CreateButton("Rainbow Hitbox: OFF", 320, function(self)
    RainbowHitbox = not RainbowHitbox
    self.Text = "Rainbow Hitbox: " .. (RainbowHitbox and "ON" or "OFF")
end)

-- AIM ASSIST MEJORADO
RunService.RenderStepped:Connect(function()
    if not AimAssistEnabled then return end
    local closest, dist = nil, math.huge
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("Head") then
            local head = plr.Character.Head
            local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
            if onScreen then
                local d = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if d < dist then 
                    dist = d 
                    closest = head.Position 
                end
            end
        end
    end
    if closest then
        local cf = Camera.CFrame
        Camera.CFrame = cf:Lerp(CFrame.lookAt(cf.Position, closest), AimPower/140)
    end
end)

-- ESP + WALLHACK
RunService.RenderStepped:Connect(function()
    if not ESPEnabled then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            if not plr.Character:FindFirstChild("HighlightESP") then
                local hl = Instance.new("Highlight")
                hl.Name = "HighlightESP"
                hl.FillTransparency = 0.7
                hl.OutlineColor = Color3.fromRGB(255, 50, 50)
                hl.FillColor = Color3.fromRGB(0, 255, 255)
                hl.Parent = plr.Character
            end
        end
    end
end)

-- HITBOX EXPAND (INSANE)
local rainbowHue = 0
RunService.RenderStepped:Connect(function()
    if not HitboxEnabled then return end
    rainbowHue = (rainbowHue + 0.01) % 1
    local color = RainbowHitbox and Color3.fromHSV(rainbowHue, 1, 1) or Color3.fromRGB(255, 0, 0)
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
            local head = plr.Character:FindFirstChild("Head")
            if hrp then
                hrp.Size = Vector3.new(10, 10, 10)
                hrp.Transparency = 0.6
                hrp.Color = color
                hrp.CanCollide = false
            end
            if head then
                head.Size = Vector3.new(6, 6, 6)
                head.Transparency = 0.6
                head.Color = color
            end
        end
    end
end)

-- TRIGGERBOT 0.1ms
RunService.RenderStepped:Connect(function()
    if not TriggerBotEnabled then return end
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("Head") then
            local head = plr.Character.Head
            local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
            if onScreen then
                local distance = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).Magnitude
                if distance < 75 then
                    VirtualInputManager:SendMouseButtonEvent(0,0,0,true,game,1)
                    task.wait(0.0001) -- 0.1 milisegundo
                    VirtualInputManager:SendMouseButtonEvent(0,0,0,false,game,1)
                    break
                end
            end
        end
    end
end)

UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.P then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

print("✅ dbgoon22 Client - Mejorado | Presiona P")
